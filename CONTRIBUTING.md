# 贡献指南

欢迎提 issue 与 PR。请先花两分钟看完本文 —— 本项目的边界与一般项目不同。

## 一、先确认资格

本项目**仅限海南师范大学本校师生**用于本人自用：须持有校园账号并能通过学校统一身份认证（CAS）登录。
没有校园账号无法获取凭据，服务也无法工作。

## 二、不会被接受的改动

以下方向**不会合并**，提之前请勿浪费时间：

- 对外监听、`0.0.0.0` 绑定、端口转发、为他人中转请求
- 多人共用凭据、多 Key 分摊额度、账号池 / 多人分发
- 利用学校账号资源从事经营性活动
- 删除或弱化 README / AGENT.md 中的风险提示与红线条款

判断标准很简单：**任何让"别人的请求以某个人的账号身份发出"的能力都不在本项目范围内。**
本程序始终只监听 `127.0.0.1`，这点是设计前提，不是待办项。

## 三、两条别动的既有设计

改动前请知悉，这两处是刻意如此，不是"待优化"的代码：

1. **限流退避梯度**（`base × 2^档位`，上限档 384 秒）—— 刻意设计的指数退避，不要改成线性或压低封顶值。
2. **模型名不写死** —— 上游返回什么就用什么，配置里的 `default_model` / `aliases` / `static_models` 保持留空；
   上游改名时由 `resolve_model()` 与自愈逻辑处理。把具体模型名硬编码进代码会造成上游一改名就全体失效。

## 四、提交前自查

- [ ] **不含凭据**：`token.txt`、`user_name.txt`、`config.json` 里的自定义 Key、任何 JWT —— 全部不得入库
- [ ] **不含运行产物**：`*.log`、`usage_log.jsonl`、`savings_state.json`、`__pycache__/`、`runtime/`
- [ ] **不含个人信息**：本机用户名、绝对路径、真实姓名
- [ ] 语法校验通过：涉及 `.py` 时至少跑一次 `runtime\python.exe -m py_compile <文件>`
- [ ] 改过 bat / 脚本后，跑一次 `3.自检.bat`；改过打包相关的还要跑 `build_portable.py`（它会校验文件引用完整性）

## 五、文档同步

- 在 README 里**新增或改名脚本**时，同步更新 `build_portable.py` 的 `FILES` 白名单，否则分发包会缺件。
- 面向 AI Agent 的内容写在 `AGENT.md`，使用者教程写在 `README.md` —— 不要混在一起膨胀 README。

## 六、提交信息

无强制规范，写清"改了什么、为什么"即可。一次提交聚焦一件事。

安全相关的问题**不要在 issue 里公开报告**，见 [`SECURITY.md`](SECURITY.md)。
