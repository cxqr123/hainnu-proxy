# 安全策略

## 报告安全问题

若发现**可能导致他人凭据泄露、本地服务被非授权访问**的问题，请**不要用公开 issue 报告**：

- 首选：仓库 **Security → Report a vulnerability**（私有安全公告，仅维护者可见）
- 或邮件：<sapphirestar46@outlook.com>

请附上：复现步骤、影响范围、环境（Windows 版本、是否使用便携 runtime）。
**切勿附带**：`token.txt` 的内容、登录 JWT、`debug_capture.log`、含绝对路径的日志 —— 这些一旦贴出即等同交出账号。

## 不属于安全问题的情况

| 现象 | 归属 |
| --- | --- |
| 令牌过期 / `401` / `auth_expired` | 凭据失效，重新执行 `1.获取令牌.bat`，见 README 故障排查 |
| 上游返回 `429` | 学校侧限流，服务会自动退避重试，见 README 限制与取值 |
| 上游模型改名 | 服务会自动重新解析模型名，无需处理 |

## 使用者侧的自我保护

1. **不要改监听地址** —— 默认 `127.0.0.1`，只有本机程序能连。若被改成对外地址且沿用公开默认 Key，服务会**拒绝启动**。
2. **默认 API Key 只适用于仅监听回环时** —— `sk-hainnu` 是写在文档里的公开值；本机自用无需改，一旦要放开监听就必须换成随机字符串。
3. **排查完关闭 `debug_capture`** —— 开启后客户端发来的**完整请求正文会明文落盘**到 `debug_capture.log`。

## 本项目已有的安全设计

- 登录令牌以 **Windows DPAPI** 加密落盘（`enc:` 前缀，仅当前 Windows 用户可解），拷到别的机器或用户即失效。
- **非回环监听 + 公开默认 Key → 拒绝启动**，避免误把自己的校园账号开放给同网段。
- `/health` 的 `probe=1`（会向上游真实发一次请求）**仅接受本机触发**，且不看可伪造的 `X-Forwarded-For`。
- 常规日志只记模型名、是否流式、消息条数、耗时、限流事件，**不记请求正文**。

## 使用边界

本程序不提供也不鼓励账号共享或请求中转；相关的对外监听、多用户分发能力在本项目中本就不存在。
请求以本人校园账号身份发出，行为均记录在本人名下 —— 详见 README「许可与声明」。
